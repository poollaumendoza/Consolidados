@model IEnumerable<Consolidados.Web.Models.ClasificacionTipoDocumento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreClasificacionTipoDocumento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado.NombreEstado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreClasificacionTipoDocumento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado.NombreEstado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdClasificacionTipoDocumento }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdClasificacionTipoDocumento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdClasificacionTipoDocumento })
        </td>
    </tr>
}

</table>
